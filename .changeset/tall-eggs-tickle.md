---
"@nodeboot/encore-server": minor
---

Add first version of Encore.ts server adapter for Node-Boot, exposing a raw endpoint handler that routes requests through Node-Boot's engine (controllers, DI, middleware, authorization, validation) via a `find-my-way` router.
